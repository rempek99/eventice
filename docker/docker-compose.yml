version: "3.8"

services:
  postgres:
    build:
      dockerfile: ./Dockerfile
    container_name: eventice-debug-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: changeitlater
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
