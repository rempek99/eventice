version: '3.8'

services:
  postgres:
    build: .
    container_name: my-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: changeitlater
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
