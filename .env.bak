# String
POSTGRES_USER=

# String
POSTGRES_PASSWORD=

# String
POSTGRES_DB=EVENTICE_DB

# Database jdbc url
DB_URL=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${POSTGRES_DB}

# In the base64 format
JWT_SECRET=