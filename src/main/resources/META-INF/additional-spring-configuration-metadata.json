{
  "properties": [
    {
      "name": "security.jwt.secret-key",
      "type": "java.lang.String",
      "description": "The secret key used to sign the JWT tokens. It should be a long, random string to ensure security."
    },
    {
      "name": "security.jwt.expiration-time",
      "type": "java.lang.String",
      "description": "The time duration after which the JWT token will expire. Default is 3600000 milliseconds (1 hour)."
    }
  ]
}
